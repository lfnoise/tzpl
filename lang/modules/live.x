-- live.x -- JITLib-style live-coding proxies: handle-based symbolic
-- connection points over the audio engine (live.proxy) plus Event-stream
-- players targeting them (live.pattern).
--
--     import live.*;
--
-- See live/proxy.x for the proxy model and live/pattern.x for pattern
-- playback. Requires the audio engine bridge (tzpl_app).

export live.proxy.*;
export live.pattern.*;
